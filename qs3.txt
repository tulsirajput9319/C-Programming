3.Check if a number is divisible by 5.

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // 30/5 0
    int num;
    printf("Enter A number to check weather is divisible 5");
    scanf("%d",&num);
    if(num%5==0)
    {
        printf(" %d is Divisible By 5",num);
    }
    else
    {
        printf("Its Not divisble By 5");
    }

    return 0;
}
